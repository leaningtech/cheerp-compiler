# Autogenerated from /home/c/Downloads/cheerp/cheerp-compiler/compiler-rt/test/interception/Unit/lit.site.cfg.py.in
# Do not edit!

# Allow generated file to be relocatable.
from pathlib import Path
def path(p):
    if not p: return ''
    return str((Path(__file__).parent / p).resolve())


# Load common config for all compiler-rt unit tests.
lit_config.load_config(config, "/home/c/Downloads/cheerp/cheerp-compiler/compiler-rt/build/unittests/lit.common.unit.configured")

# Setup config name.
config.name = 'Interception-Unit'

# Setup test source and exec root. For unit tests, we define
# it as build directory with interception tests.
# FIXME: De-hardcode this path.
config.test_exec_root = os.path.join("/home/c/Downloads/cheerp/cheerp-compiler/compiler-rt/build", "lib",
                                     "interception", "tests")
config.test_source_root = config.test_exec_root
