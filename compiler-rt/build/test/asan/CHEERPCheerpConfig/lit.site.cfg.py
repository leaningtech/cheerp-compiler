# Autogenerated from /home/c/Downloads/cheerp/cheerp-compiler/compiler-rt/test/asan/lit.site.cfg.py.in
# Do not edit!

# Allow generated file to be relocatable.
from pathlib import Path
def path(p):
    if not p: return ''
    return str((Path(__file__).parent / p).resolve())


# Tool-specific config options.
config.name_suffix = "-cheerp-cheerp"
config.target_cflags = ""
config.clang = ""
config.bits = "32"
config.arm_thumb = ""
config.apple_platform = "osx"
config.apple_platform_min_deployment_target_flag = ""
config.asan_dynamic = False
config.target_arch = "Cheerp"
config.executor = "node"
config.linear_output = "wasm"

# Load common config for all compiler-rt lit tests.
lit_config.load_config(config, "/home/c/Downloads/cheerp/cheerp-compiler/compiler-rt/build/test/lit.common.configured")

# Load tool-specific config that would do the real work.
lit_config.load_config(config, "/home/c/Downloads/cheerp/cheerp-compiler/compiler-rt/test/asan/lit.cfg.py")
