# Autogenerated from /home/alex/cheerp/cheerp-compiler/compiler-rt/test/sanitizer_common/lit.site.cfg.py.in
# Do not edit!

# Allow generated file to be relocatable.
import os
import platform
def path(p):
    if not p: return ''
    # Follows lit.util.abs_path_preserve_drive, which cannot be imported here.
    if platform.system() == 'Windows':
        return os.path.abspath(os.path.join(os.path.dirname(__file__), p))
    else:
        return os.path.realpath(os.path.join(os.path.dirname(__file__), p))


# Tool-specific config options.
config.name_suffix = "asan-Cheerp-Cheerp"
config.tool_name = "asan"
config.target_cflags = ""
config.target_arch = "Cheerp"

config.arm_thumb = ""

# Load common config for all compiler-rt lit tests.
lit_config.load_config(config, "/home/alex/cheerp/cheerp-compiler/compiler-rt/build/test/lit.common.configured")

# Load tool-specific config that would do the real work.
lit_config.load_config(config, "/home/alex/cheerp/cheerp-compiler/compiler-rt/test/sanitizer_common/lit.common.cfg.py")

