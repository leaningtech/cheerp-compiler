# Autogenerated from /home/c/Downloads/cheerp/cheerp-compiler/compiler-rt/test/sanitizer_common/lit.site.cfg.py.in
# Do not edit!

# Allow generated file to be relocatable.
from pathlib import Path
def path(p):
    if not p: return ''
    return str((Path(__file__).parent / p).resolve())


# Tool-specific config options.
config.name_suffix = "asan-Cheerp-Cheerp"
config.tool_name = "asan"
config.target_cflags = ""
config.target_arch = "Cheerp"

config.arm_thumb = ""

# Load common config for all compiler-rt lit tests.
lit_config.load_config(config, "/home/c/Downloads/cheerp/cheerp-compiler/compiler-rt/build/test/lit.common.configured")

# Load tool-specific config that would do the real work.
lit_config.load_config(config, "/home/c/Downloads/cheerp/cheerp-compiler/compiler-rt/test/sanitizer_common/lit.common.cfg.py")

