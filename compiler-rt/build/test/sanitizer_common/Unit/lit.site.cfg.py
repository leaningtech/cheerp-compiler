# Autogenerated from /home/alex/cheerp/cheerp-compiler/compiler-rt/test/sanitizer_common/Unit/lit.site.cfg.py.in
# Do not edit!

# Allow generated file to be relocatable.
import os
import platform
def path(p):
    if not p: return ''
    # Follows lit.util.abs_path_preserve_drive, which cannot be imported here.
    if platform.system() == 'Windows':
        return os.path.abspath(os.path.join(os.path.dirname(__file__), p))
    else:
        return os.path.realpath(os.path.join(os.path.dirname(__file__), p))


# Load common config for all compiler-rt unit tests.
lit_config.load_config(config, "/home/alex/cheerp/cheerp-compiler/compiler-rt/build/unittests/lit.common.unit.configured")

# Setup config name.
config.name = 'SanitizerCommon-Unit'

# Setup test source and exec root. For unit tests, we define
# it as build directory with sanitizer_common tests.
# FIXME: De-hardcode this path.
config.test_exec_root = os.path.join("/home/alex/cheerp/cheerp-compiler/compiler-rt/build", "lib",
                                     "sanitizer_common", "tests")
config.test_source_root = config.test_exec_root

if not config.parallelism_group:
  config.parallelism_group = 'shadow-memory'
